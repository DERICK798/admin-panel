// controllers/client.controller.js

// Example function for GET /clients
exports.getClients = (req, res) => {
  res.json({ message: 'Client route works' });
};

// You can add more client-related functions here later
